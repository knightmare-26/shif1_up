# Minimal requirements for CI — skips ML packages (lazy-imported) and unused deps.
# xgboost/lightgbm are lazy-imported inside prediction_service; not exercised by tests.
# scikit-learn is listed in requirements.txt but not imported anywhere.
# asyncpg/aioredis are unused in test env (mocks cover Redis/Postgres).

# Core FastAPI and web server
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0

# F1 Data
fastf1==3.6.1
pandas==2.1.4
numpy==1.26.4

# Database
duckdb==1.1.3
pyarrow==14.0.1
asyncpg==0.29.0

# Redis (sync client only — tests fall back to MockRedisService)
redis[hiredis]==5.0.1

# HTTP and async
aiohttp==3.9.1
httpx==0.25.2

# Rate limiting
slowapi==0.1.9

# Logging
python-json-logger==4.1.0

# Data processing
pydantic==2.5.0
python-dotenv==1.0.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# Utilities
python-multipart==0.0.6
setuptools>=68.0
